# Face-Mask-Detection-System
FaceGuard AI is a full-stack Face Mask Detection system built with Django, OpenCV, Keras, HTML/CSS, and SQLite. It supports image uploads and webcam capture, detects faces, analyzes multiple facial regions using a trained ML model, and predicts Mask or No Mask for authenticated users.
